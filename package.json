{
  "name": "systemsity-chatbot-backend",
  "version": "1.0.0",
  "description": "Small proxy server to call OpenAI for the SystemSity chatbot (keeps API key on server)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "openai": "^4.5.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
